pub struct Enigma {
    dials: Vec<u32>,
    wires: Vec<(char, char)>,
}

pub impl Enigma {
    fn encrypt(&self, input: &str) -> String {
        // TODO: Make ecryption function.
        String::new()
    }
}
